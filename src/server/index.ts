import { auth } from './authService';
import { getPlayer, createPlayer } from './playerService';
import { getCards, getDeck, setDeck } from './cardService';
import { getCharacters, getClasses } from './staticService';

export {
	auth,
	getCards,
	getDeck,
	setDeck,
	getPlayer,
	createPlayer,
	getCharacters,
	getClasses,
};
